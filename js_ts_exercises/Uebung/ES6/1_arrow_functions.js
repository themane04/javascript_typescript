//Schreibe folgende Funktionen als arrow Funktion ()=>  je auf einer einzelnen Zeile;
//eine double() funktion welche das doppelte zurück gibt
//eine add() funktion, welche 2 zahlen als input nimmt, diese zusammen zählt und dann zurück gibt

export function add(x, y) {
  return x + y;
}
export function double(x) {
  return x + x;
}
